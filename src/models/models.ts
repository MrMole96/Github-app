export type User = {
  id: number;
  login: string;
};

export type Repository = {
  id: number;
  name: string;
};

export type UserDetails = {
  avatar_url: string;
  followers: number;
  id: number;
  login: string;
};
