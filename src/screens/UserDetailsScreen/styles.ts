import { StyleSheet } from "react-native";

export default StyleSheet.create({
  image: { width: "100%", aspectRatio: 1, height: undefined },
  detailsText: { fontWeight: "bold", marginLeft: 5 },
  flexRowDirection: { flexDirection: "row" },
});
